{% extends 'base.html.twig' %}

{% block title %}Editer Artiste{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/form.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="my-4">Editer Artiste</h1>

    {{ include('creator/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('creator/_delete_form.html.twig') }}

    <a href="{{ path('creator_index') }}" type="btn" class="btn btn-outline-secondary">Retour à la Liste</a>
{% endblock %}
