{% extends 'base.html.twig' %}

{% block title %}Editer Artiste / Auteur{% endblock %}

{% block body %}
    <h1>Editer Artiste / Auteur</h1>

    {{ include('creator/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('creator_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    {{ include('creator/_delete_form.html.twig') }}
{% endblock %}
