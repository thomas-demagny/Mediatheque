{% extends 'base.html.twig' %}

{% block title %}Ressource{% endblock %}

{% block body %}
    <h1 class="my-4">Ressource</h1 class="my-4">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ resource.url }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ resource.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resources_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('resources_edit', {'id': resource.id}) }}">Editer</a>

    {{ include('resources/_delete_form.html.twig') }}
{% endblock %}
