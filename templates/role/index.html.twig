{% extends 'base.html.twig' %}

{% block title %}Liste des Roles{% endblock %}

{% block body %}
    <h1 class="my-4">Liste des Roles</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td>{{ role.id }}</td>
                <td>
                    <a href="{{ path('role_show', {'id': role.id}) }}">Voir</a>
                    <a href="{{ path('role_edit', {'id': role.id}) }}">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">Aucune entitée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('role_new') }}" type="btn" class="btn btn-outline-secondary my-3">Ajouter nouveau</a>
{% endblock %}
