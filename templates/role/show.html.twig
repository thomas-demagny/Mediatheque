{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1 class="my-4">Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('role_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('role_edit', {'id': role.id}) }}">Editer</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
