{% extends 'base.html.twig' %}

{% block title %}Employé{% endblock %}

{% block body %}
    <h1 class="my-4">Employé</h1 class="my-4">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('employee_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('employee_edit', {'id': employee.id}) }}">Editer</a>

    {{ include('employee/_delete_form.html.twig') }}
{% endblock %}
