<form method="post" action="{{ path('dvd_delete', {'id': dvd.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dvd.id) }}">
    <button type="btn" class="btn btn-danger">Supprimer</button>
</form>
