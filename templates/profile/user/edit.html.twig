{% extends 'base.html.twig' %}

{% block title %}Editer Utilisateur{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/form.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="my-4">Modification Utilisateur</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <a href="{{ path( 'user_change_password') }}" type="btn" class="btn btn-success">Modifier le mot de passe</a>
    {{ include('user/_delete_form.html.twig') }}

    <a href="{{ path('user_index') }}" type="btn" class="btn btn-outline-secondary">Retour à la Liste</a>
{% endblock %}
