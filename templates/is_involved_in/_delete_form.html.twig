<form method="post" action="{{ path('is_involved_in_delete', {'id': is_involved_in.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ is_involved_in.id) }}">
    <button type="btn" class="btn btn-danger">Supprimer</button>
</form>
