{% extends 'base.html.twig' %}

{% block title %}IsInvolvedIn{% endblock %}

{% block body %}
    <h1 class="my-4">IsInvolvedIn</h1 class="my-4">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ is_involved_in.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ is_involved_in.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('is_involved_in_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('is_involved_in_edit', {'id': is_involved_in.id}) }}">Editer</a>

    {{ include('is_involved_in/_delete_form.html.twig') }}
{% endblock %}
