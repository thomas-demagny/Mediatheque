{% extends 'base.html.twig' %}

{% block title %}Editer Maintenance{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/form_general.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="my-4">Editer Maintenance</h1>

    {{ include('maintenance/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('maintenance/_delete_form.html.twig') }}

    <a href="{{ path('maintenance_index') }}" type="btn" class="btn btn-outline-secondary">Retour à la Liste</a>
{% endblock %}
