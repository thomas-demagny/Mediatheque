{% extends 'base.html.twig' %}

{% block title %}Maintenance{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/show_entity.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="my-4">Maintenance</h1>

    <table class="table text-center">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maintenance.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ maintenance.status }}</td>
            </tr>
            <tr>
                <th>Date Maintenance</th>
                <td>{{ maintenance.maintenanceDate ? maintenance.maintenanceDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    
    <div class="actions my-3">
        <a href="{{ path('maintenance_edit', {'id': maintenance.id}) }}" type="btn" class="btn btn-success">Editer</a>
        {{ include('maintenance/_delete_form.html.twig') }}
        <a href="{{ path('maintenance_index') }}" type="btn" class="btn btn-outline-secondary">Retour à la Liste</a>
    </div>
{% endblock %}
