{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
    <h1 class="my-4">Livre</h1 class="my-4">

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ book.id }}</td>
        </tr>
        <tr>
            <th>Pages</th>
            <td>{{ book.pages }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('book_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('book_edit', {'id': book.id}) }}">Editer</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
