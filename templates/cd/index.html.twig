{% extends 'base.html.twig' %}

{% block title %}Cd index{% endblock %}

{% block body %}
    <h1>Cd index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>

                <th>Plages</th>
                <th>Duration</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in cds %}
            <tr>
                {{ include ("layout/globalProduct.html.twig",{'product': product } ) }}
                <td>{{ product.plages }}</td>
                <td>{{ product.duration ? product.duration|date('H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('cd_show', {'id': product.id}) }}">show</a>
                    <a href="{{ path('cd_edit', {'id': product.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cd_new') }}">Create new</a>
{% endblock %}
