{% extends 'base.html.twig' %}

{% block title %}CD{% endblock %}

{% block body %}
    <h1 class="my-4">CD</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cd.id }}</td>
            </tr>
            <tr>
                <th>Plages</th>
                <td>{{ cd.plages }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ cd.duration ? cd.duration|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cd_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('cd_edit', {'id': cd.id}) }}">Editer</a>

    {{ include('cd/_delete_form.html.twig') }}
{% endblock %}
