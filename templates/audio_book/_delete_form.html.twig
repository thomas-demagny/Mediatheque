<form method="post" action="{{ path('audio_book_delete', {'id': audio_book.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ audio_book.id) }}">
    <button type="btn" class="btn btn-danger">Supprimer</button>
</form>
