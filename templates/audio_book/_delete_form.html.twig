<form method="post" action="{{ path('audio_book_delete', {'id': audio_book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ audio_book.id) }}">
    <button class="btn">Delete</button>
</form>
