{% extends 'base.html.twig' %}

{% block title %}AudioBook index{% endblock %}

{% block body %}
    <h1>Livres audio index</h1>

    <table class="table text-center">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code produit</th>
                <th>En stock</th>
                <th>Format</th>
                <th>Genre</th>
                <th>Auteur</th>
                <th>Titre</th>
                <th>Durée</th>
                <th>actions</th>

            </tr>
        </thead>
        <tbody class="text-center">
        {% for product in audio_books %}
            <tr>
                {{ include ("layout/globalProduct.html.twig",{'product': product } ) }}
                <td>{% for product in product.isInvolvedIns %}
                        {{ product.creator}}
                    {% endfor %}
                </td>

                <td>{{ product.duration ? product.duration|date('H:i:s') : '' }}
                </td>
                <td>
                    <a href="{{ path('audio_book_show', {'id': product.id}) }}" type="btn" class="btn btn-success">Voir</a>
                    <a href="{{ path('audio_book_edit', {'id': product.id}) }}" type="btn" class="btn btn-secondary">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('audio_book_new') }}" type="btn" class="btn btn-dark">Création</a>
{% endblock %}





