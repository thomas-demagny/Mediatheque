{% extends 'base.html.twig' %}

{% block title %}DVD{% endblock %}

{% block body %}
    <h1 class="my-4">Dvd</h1 class="my-4">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dvd.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dvd_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('dvd_edit', {'id': dvd.id}) }}">Editer</a>

    {{ include('dvd/_delete_form.html.twig') }}
{% endblock %}
