{% extends 'base.html.twig' %}

{% block title %}E-Book{% endblock %}

{% block body %}
    <h1 class="my-4">E-Book</h1 class="my-4">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ebook.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ebook_index') }}" type="btn" class="btn btn-outline-secondary my-3">Retour à la Liste</a>

    <a href="{{ path('ebook_edit', {'id': ebook.id}) }}">Editer</a>

    {{ include('ebook/_delete_form.html.twig') }}
{% endblock %}
