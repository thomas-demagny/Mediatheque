{% extends 'base.html.twig' %}

{% block title %}CD{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/show_entity.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="my-4">CD</h1>

    <table class="table text-center">
        <tbody>
            <tr>
                                <td>{{ cd.id }}</td>
            </tr>
            <tr>
                <th>Plages</th>
                <td>{{ cd.plages }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ cd.duration ? cd.duration|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="actions my-3">
        <a href="{{ path('cd_edit', {'id': cd.id}) }}" type="btn" class="btn btn-success">Editer</a>
        {{ include('cd/_delete_form.html.twig') }}
        <a href="{{ path('cd_index') }}" type="btn" class="btn btn-outline-secondary">Retour à la Liste</a>
    </div>
{% endblock %}
