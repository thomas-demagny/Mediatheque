{% extends 'base.html.twig' %}

{% block title %}E-Book{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/show_entity.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="my-4">E-Book</h1>

    <table class="table text-center">
        <tbody>
            <tr>
                                <td>{{ ebook.id }}</td>
            </tr>
        </tbody>
    </table>

    <div class="actions my-3">
        <a href="{{ path('ebook_edit', {'id': ebook.id}) }}" type="btn" class="btn btn-success">Editer</a>
        {{ include('ebook/_delete_form.html.twig') }}
        <a href="{{ path('ebook_index') }}" type="btn" class="btn btn-outline-secondary">Retour à la Liste</a>
    </div>
{% endblock %}
